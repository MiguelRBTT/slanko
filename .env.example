# Copy to .env and adjust if needed (do not commit .env)
DATABASE_URL="mysql://slanko:slanko@localhost:3306/slanko"

# JWT auth (use a long random string in production)
JWT_SECRET="change-me-to-a-long-random-secret"
JWT_EXPIRES_IN="8h"
